describe('redirect', () => {
  it('should redirect from / to /login when logged out', () => {});
});

describe('redirect', () => {
  it('should redirect from / to /moves when logged in', () => {});
});
