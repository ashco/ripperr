import styled from 'styled-components';
import ModalContainer from 'domain/Modal/ModalContainer';

const DeleteMovementMenuContainer = styled(ModalContainer)`
  .content {
    display: grid;
    gap: 1rem;
  }
`;

export default DeleteMovementMenuContainer;
