import styled from 'styled-components';

const Form = styled.form`
  display: grid;
  gap: 2rem;
  /* width: 24rem; */

  .input-container {
    display: grid;
    gap: 0.5rem;
  }
`;

export default Form;
