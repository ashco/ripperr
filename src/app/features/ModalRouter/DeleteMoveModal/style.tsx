import styled from 'styled-components';
import Container from 'components/Container';

const DeleteMoveModalContainer = styled(Container)`
  width: 28rem;
  .content {
    display: grid;
    gap: 1rem;
  }
`;

export default DeleteMoveModalContainer;
