import React from 'react';
import { render } from 'test/test-utils';
import MovementForm from '../index';

import { ModalMode } from 'types/enums';

test('should render', () => {
  // render(<MovementForm mode={ModalMode.View} />);
});
