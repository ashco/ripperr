import styled from 'styled-components';
import ModalContainer from 'components/ModalContainer';

const DeleteMovementModalContainer = styled(ModalContainer)`
  .content {
    display: grid;
    gap: 1rem;
  }
`;

export default DeleteMovementModalContainer;
